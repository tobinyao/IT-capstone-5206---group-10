# Backend environment variables
#
# Copy these values into your shell, local process manager, or hosting
# provider environment settings. Do not commit real secrets.

# Internal secret required by POST /api/register.
# Requests that create accounts must send the same value in the
# X-Admin-Secret header, otherwise the backend returns 403.
ADMIN_REGISTER_SECRET=replace-with-your-internal-secret
